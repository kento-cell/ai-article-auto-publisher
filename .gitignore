# 環境変数
.env

# 認証情報 / シークレット
config/credentials.json
ai-publisher-*.json
*-service-account*.json
*.pem
*.key
config/settings.yaml

# ブラウザ永続セッション（note.com / Selenium / Playwright / Brave CDP）
# — 認証済みCookieやIndexedDBが含まれるため絶対にコミットしない
data/note-profile/
data/selenium-profile/
.brave-cdp-profile/
*_cookies.json
cookies/

# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
env/
venv/
.venv/

# ログ
logs/
*.log

# データ
data/
*.db
*.sqlite

# 生成画像
docs/images/covers/
docs/images/diagrams/

# OS
.DS_Store
Thumbs.db

# IDE
.vscode/
.idea/
*.swp

# Claude Code ローカル状態
.claude/scheduled_tasks.lock
.claude/settings.local.json

# 一時ワークスペース
tmp_img/
run_generate.sh

# Owner-specific PII patterns used by scripts/_pii_scan_diff.py
# (the scanner itself is public; the patterns are not)
config/pii_scan_extra.json

# Intermediate diagram PNGs for the AI tech spec workbook
# (regenerated by scripts/_ai_spec_diagrams.py and embedded into the .xlsx)
docs/_spec_assets/
